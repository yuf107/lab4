<!doctype html>

<html>
<head>
<!-- <script src="../js/add.js"></script> -->
<link href="/css/addevent.css" rel="stylesheet">
<meta charset = "utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>

<body>
	
		<div class="title">
			<a href="index">
				<img src="/images/backbutton.png" alt="back" width="24" height="30" ></a>
				<h2>Add or Edit Event</h2></div>
<div align="center">
		<div class="container"> 
		<form method="get" action="{{action}}">

			<h4>Event name:</h4>
			<h5>{{message}}</h5>
			<input name="name" value="{{name}}" {{readonly}}>
				<!-- h6 style="display:inline">{{message}}</h6-->
			</input>

			<h4>Comments:</h4>
			<input name="comment" value="{{comment}}"></input>	
			<h4>Date:</h4>
			<input type="date" name="date" value="{{date}}"></input>
			<h4>Time:</h4>
			<input type="time" name="timeFrom" value="{{timeFrom}}"></input>
			to
			<input type="time" name="timeTo" value="{{timeTo}}"></input>
			<h4>Location:</h4>
			<input name="place" value="{{place}}"></input>
			<h4>Importance:</h4>
			<select name="importance" value="{{importance}}">
				<option value="32">One Star</option>
				<option value="48">Two Star</option>
				<option value="65">Three Star</option>
			</select>
		</br>
			<!-- form action="add.handlebars" method="post" enctype="multipart/form-data">
				<label for="file"> Choose a photo:</label>
					<input type="file" name="file1"/>
					<input type="submit" value="Use photo!"/>-->
		<!--	<h4>Choose photo</h4>
			<form name="uploaded">
				<input type="file" size="25" accept="image/*">
				<input type="Submit"></form>-->
		<div style="text-align:center;">
		<input type="submit" id="submit" class="btn btn-default" value="{{edit}}" style="width:80%"></input> <!-- One button switch text -->
		
		<!-- Button class="btn"style="width:80%">Edit Event</Button -->

		</br>
		<Button class="btn1">Invite Friends </Button>
		</br>
		<Button class="btn2">Archive </Button>
		<Button class="btn3">Delete </Button>
		</div>
	</div>
</div>	
</body>
</html>
